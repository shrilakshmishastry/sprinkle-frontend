const initialState ={
    productsInHomePage : [],
};

export default function HomePage(state = initialState,action){
    switch(action.type){
        default: {
            return state;
        }
    }
}