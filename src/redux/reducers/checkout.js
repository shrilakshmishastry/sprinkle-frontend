const initialState ={
    productsAtCart : [],
};

export default function CheckOut(state = initialState,action){
    switch(action.type){
        default: {
            return state;
        }
    }
}